import React, { type ReactNode } from 'react'

export type WindowFrameProps = {
  children: ReactNode
}

const WindowFrame = ({}: WindowFrameProps) => {
  return <div></div>
}

export default WindowFrame
